# =================== 网络配置 =========================
# 生产环境使用同源根路径请求后端接口，不配置 VITE_APP_BASE_API。
# 例如前端会直接请求 /api/template/status。
# 如果生产环境确实需要统一请求前缀，可显式开启：
# VITE_APP_BASE_API=/api

# 项目名称。
# 可用于页面标题、应用内展示文案或后续构建信息展示。
# 修改这里只影响前端读取到的应用名称，不等同于 Tauri 原生 productName。
# 默认值：Tauri Vue Examples。
VITE_APP_TITLE=Tauri Vue Examples

# 后端接口地址。
# Web 同源部署时保持为空。
# Tauri App 生产包、Android/iOS 或其它需要直连后端的场景，应配置真实 http/https 绝对地址。
# 默认值：不设置。
VITE_APP_API_URL=
# VITE_APP_API_URL=https://api.example.com
# VITE_APP_API_URL=https://api.example.com/v2

# 原生 upload 插件示例上传地址。
# upload 是原生 HTTP 请求，不走浏览器相对路径；生产环境必须按真实上传服务配置。
# 不配置时上传示例会明确提示缺少上传地址，不会伪装成功。
# 默认值：不设置。
VITE_UPLOAD_DEMO_URL=
# VITE_UPLOAD_DEMO_URL=https://api.example.com/api/template/upload

# =================== 本地存储配置 =========================
# Pinia 持久化 key 前缀。
# 用于区分不同项目或环境的本地缓存，避免 localStorage/sessionStorage 数据互相污染。
# 修改后旧缓存不会自动迁移，相当于使用一套新的前端持久化命名空间。
# 默认值：tauri-examples。
VITE_STORE_KEY_PREFIX=tauri-examples

# =================== 生产辅助配置 =========================
# Mock 服务开关。
# true 表示启用本地 Mock 数据，false 表示请求真实接口。
# 生产环境必须关闭，避免静态包误用模板 Mock。
# 默认值：false。
VITE_MOCK_DEV_SERVER=false

# HTTP 调试日志开关。
# true 时前端会输出 [api:request]、[api:response]、[api:error] 等接口调试日志。
# 生产环境必须关闭，避免泄露请求信息。
# 默认值：false。
VITE_APP_DEBUG=false

# UnoCSS 配置调试开关。
# true 时会把 UnoCSS resolved config 写入 backup/unocss，便于排查 preset、theme、variant 和 rules 的合并结果。
# 生产环境保持关闭。
# 默认值：false。
VITE_DEBUG_UNOCSS_CONFIG=false

# =================== 路由配置 =========================
# 路由来源。
# local：只使用前端本地静态路由定义。
# remote：后续业务可扩展为后端下发动态路由。
# hybrid：后续业务可扩展为远端优先、本地兜底。
# 默认值：local。
VITE_ROUTE_SOURCE=local
# VITE_ROUTE_SOURCE=remote
# VITE_ROUTE_SOURCE=hybrid
